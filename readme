Como rodar o projeto

Instalar os mudolo e necessario o node.js

Instalar o modulo Sequelize:
npm install --save sequelize

Instalar o modulo Mysql 2:
npm install --save mysql2

Instalar o modulo Nodemon para rodar o servidor e ir editando sem matar a aplicacao:
npm install nodemon -g

Instalar o modulo do express:
npm install express

Instalar o modulo body-parser:
npm install --save body-parser

Instalar o modulo Handlebar:
npm install --save express-handlebars

Instalar o módulo para criptografar a senha
### npm install --save bcryptjs

Instalar a dependencia para JWT
### npm install --save jsonwebtoken